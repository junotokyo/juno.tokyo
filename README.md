# PopScan Website

Static website for `https://juno.tokyo/popscan`.

Upload these files to the `popscan` directory on the web server:

- `index.html`
- `styles.css`
- `script.js`
- `robots.txt`
- `assets/popscan-icon-512.png`
- `assets/app-store-badge-jp.svg`
- `assets/cafe-open-flyer.png`

The page includes a Japanese-first product overview, English language toggle, support contact, and privacy policy for App Store review.

## Preview in Codex

This site can be previewed directly in the Codex in-app browser because it is a static page.

Ask Codex:

```text
Codex内ブラウザで index.html を開いて表示確認して
```

Codex should open:

```text
file:///Users/jokamoto/Library/CloudStorage/Dropbox/Codex/PopScan/index.html
```

After design changes, ask Codex to reload the page and check the desktop/mobile layout with screenshots.
